{
  "name": "altera",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync-env": "node scripts/sync-env.mjs",
    "dev": "node scripts/sync-env.mjs && npm run dev --prefix frontend",
    "dev:frontend": "node scripts/sync-env.mjs && npm run dev --prefix frontend",
    "dev:convex": "npx convex dev",
    "build": "node scripts/sync-env.mjs && npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "convex": "^1.39.1"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
